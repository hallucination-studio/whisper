{
  "name": "whisper-read-only-ui",
  "private": true,
  "scripts": {
    "evidence:observe": "node scripts/evidence-observer.mjs",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
